import "./globals.css";
export const metadata={title:"Photo.is.a.Story — Fine Art Photography",description:"Limited edition fine art photography"};
export default function Layout({children}:{children:React.ReactNode}){return <html lang="en"><body>{children}</body></html>}
